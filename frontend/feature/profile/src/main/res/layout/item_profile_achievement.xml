<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="8dp">

    <FrameLayout
        android:id="@+id/achievement_badge_container"
        android:layout_width="78dp"
        android:layout_height="72dp"
        android:background="@drawable/bg_achievement_hexagon_locked">

        <TextView
            android:id="@+id/achievement_milestone_value"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="1"
            android:textColor="@color/text_primary"
            android:textSize="22sp"
            android:textStyle="bold" />
    </FrameLayout>

    <TextView
        android:id="@+id/achievement_milestone_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="1 activity"
        android:textColor="@color/text_secondary"
        android:textSize="12sp" />
</LinearLayout>
